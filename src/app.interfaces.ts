export enum Instrument {
	GUITAR = 'guitar',
	DRUMS = 'drums',
	VOICE = 'voice',
	BAS = 'bas',
	KEYS = 'keys',
	SAX = 'sax',
	TRUMPET = 'trumpet',
	TROMBONE = 'trombone',
	BAND = 'band',
	OTHER = 'other',
}

export enum Gender {
	M  = 'm',
	F = 'f',
}

export enum UserType {
	MUSICIAN = 'musician',
	MANAGER = 'manager',
}

export enum FeedType {
	JOB = 'job',
	USER = 'user',
}